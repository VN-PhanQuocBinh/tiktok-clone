import classNames from "classnames/bind";
import styles from "../../../assets/styles/components/Modals/ui/Alert.module.scss";

const cx = classNames.bind(styles);

function Alert() {

   
   return (
      <>
      
      </>
   );
}

export default Alert;